## ZDays iOS StateMachines

A playground for my ZDays iOS StateMachines talk.

### Getting started

* Clone this repo
* `cd` into the repo directory
* Run `git submodule init`
* Open `zdaysform.workspace`
* Build `cmd+B`
* Open and run the playground.
